SIMD 
    used everywhere
    within processors

MIMD
    across processors

SPMD = Single program multiple data
    multiple cores, each core takes a subset, then combine results

SIMD (lower level) 
    to do more work on higher level data that is broken down
    - scientific computing

